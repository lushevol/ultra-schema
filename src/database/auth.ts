import type { LoginResponse } from 'src/authentication/types/request';

export const mockLoginResponse: LoginResponse = {
  entities: [],
  entitlementsToken: null,
  errorMessage: null,
  expiration: null,
  oud: null,
  result: true,
  userInfo: '{"sub":"1639796"}',
};
